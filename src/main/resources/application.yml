server:
  port: 3001
  servlet:
    context-path: /api/private/v1

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    # 新版mysql必须加时区
    url: jdbc:mysql://localhost:3306/vsdb?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: root
